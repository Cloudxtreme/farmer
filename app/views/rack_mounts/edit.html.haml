.panel.panel-default
  .panel-heading
    %h4.panel-title
      .btn-group
        %a.btn.btn-primary{ href: rack_mount_path( @rack_mount ) }
          %span.glyphicon.glyphicon-chevron-left
        %a.btn.btn-default{ href: "#" }
          %b= "Editing #{@rack_mount.name}"
        %a.btn.btn-danger{ href: rack_mount_path( @rack_mount ), rel: 'nofollow', :'data-method' => 'delete', :'data-confirm' => 'Are you sure?' }
          %span.glyphicon.glyphicon-trash

= render 'form'
